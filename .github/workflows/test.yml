name: triggered test

on:
  repository_dispatch:
    types: 
      - manual-trigger-test"
jobs:
        push-to-branches:
          runs-on: ubuntu-latest
          steps:

            - name: Print Hola
              run: echo "hola"