name: Create Release

on:
  workflow_dispatch:
    inputs: 
      # Release:
      #   description: 'Would you like to create a release?'
      #   required: true
      #   default: 'No'
      #   type: choice
      #   options:
      #     - 'Yes'
      #     - 'No'
      commit:
        description: 'Commit ID to deploy'
        required: false
      version:
        description: 'Version to deploy'
        required: false
      
jobs:
  define-outputs:
    runs-on: ubuntu-latest
    outputs:
      commit: ${{ inputs.commit }}
      version: ${{ inputs.version }}
    uses: ./.github/deploy-jar.yml
  
  push-to-branches:
          runs-on: ubuntu-latest
          steps:
            - name: Imprimir los valores de los inputs
              run: |
                echo "¿Es un release? ${{ inputs.is_release }}"
                echo "Versión a desplegar: ${{ inputs.version }}"